import { v4 as uuid } from 'uuid'
import { Factory, Seeder } from 'typeorm-seeding'
import { Connection } from 'typeorm'

import {{pascalCase entity}} from '@modules/{{camelCase moduleName}}/infra/typeorm/entities/{{pascalCase entity}}.entity'

export default class {{pascalCase name}} implements Seeder {
  public async run (factory: Factory, connection: Connection): Promise<any> {
    await connection
      .createQueryBuilder()
      .insert()
      .into({{pascalCase entity}})
      .values([
        {
          id: uuid(),
          props: 'values'
        }
      ])
      .execute()

    connection.close()
  }
}
