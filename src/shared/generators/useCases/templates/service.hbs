import { inject, injectable } from 'tsyringe'
// import AppError from '@shared/errors/AppError'

import {{pascalCase useCaseName}} from '@modules/{{camelCase moduleName}}/infra/typeorm/entities/{{pascalCase useCaseName}}'
import I{{pascalCase moduleName}}Repository from '@modules/{{camelCase moduleName}}/repositories/interfaces/I{{pascalCase moduleName}}Repository'

interface Request{
  data: string
}

@injectable()
export default class {{pascalCase useCaseName}}{{pascalCase actionName}}Service {
  constructor (
    @inject('{{pascalCase moduleName}}Repository')
    private repository: I{{pascalCase moduleName}}Repository
  ) {}

  async execute ({ data }: Request): Promise<{{pascalCase useCaseName}}> {
    // To Do
  }
}
