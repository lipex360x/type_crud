// snippet: controllerTemplate
import { Request, Response } from 'express'
import { container } from 'tsyringe'

import {{pascalCase name}}CreateService from '@modules/{{camelCase name}}/useCases/{{pascalCase name}}/Create/{{pascalCase name}}CreateService'

export default class Create{{pascalCase name}}Controller {
  async handle (request: Request, response: Response): Promise<Response> {
    const { props } = request.body

    const service = container.resolve({{pascalCase name}}CreateService)

    const {{name}}CreateService = await service.execute({ props })

    return response.json({{name}}CreateService)
  }
}
