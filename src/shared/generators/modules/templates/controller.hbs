import { Request, Response } from 'express'
import { container } from 'tsyringe'

import {{pascalCase useCaseName}}{{pascalCase actionName}}Service from './{{pascalCase useCaseName}}{{pascalCase actionName}}.service'

export default class {{pascalCase useCaseName}}{{pascalCase actionName}}Controller {
  async handle (request: Request, response: Response): Promise<Response> {
    const { props } = request.body

    const service = container.resolve({{pascalCase useCaseName}}{{pascalCase actionName}}Service)

    const serviceResponse = await service.execute({ props })

    return response.json(serviceResponse)
  }
}
