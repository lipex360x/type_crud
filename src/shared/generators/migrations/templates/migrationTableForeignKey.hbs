import { MigrationInterface, QueryRunner, TableColumn, TableForeignKey } from 'typeorm'

export default class {{pascalCase name}}{{timestamp}}
implements MigrationInterface {
  public async up (queryRunner: QueryRunner): Promise<void> {
    await queryRunner.addColumn(
      '{{database}}',
      new TableColumn({
        name: '{{columnName}}',
        type: 'uuid',
        isNullable: true
      })
    )

    await queryRunner.createForeignKey(
      '{{database}}',
      new TableForeignKey({
        name: '{{database}}_to_{{tableReference}}',
        columnNames: ['{{columnName}}'],

        referencedTableName: '{{tableReference}}',
        referencedColumnNames: ['{{tableColumnReference}}'],

        onDelete: 'CASCADE',
        onUpdate: 'CASCADE'
      })
    )
  }

  public async down (queryRunner: QueryRunner): Promise<void> {
    await queryRunner.dropForeignKey('{{database}}', '{{database}}_to_{{tableReference}}')
    await queryRunner.dropColumn('{{database}}', '{{columnName}}')
  }
}
